# user configuration of cyclops
from cyclops import config

# folders
# location of panels 
config['panels_dirs'] = [
    'panels', 'source/panels' ]
    
# location of plots
config['plots_dirs'] = [
    'plots', 'source/plots' ]

# appearance
config['plot_colors'] = {
	'colorplot_cmap': 'hot',
}